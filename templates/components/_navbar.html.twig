<nav>
    <div class="navbar">
        {% if app.user %}
            <div class="linknav">
                <div class="linkAdmin">
                    <a class="btn btn-secondary btn-admin" href="{{ path('app_admin_instrument_index') }}">Liste des instruments</a>
                </div>
                <div class="linkAdmin">
                    <a class="btn btn-secondary btn-admin" href="{{ path('app_admin_comment_index') }}">Liste des commentaires</a>
                </div>
            </div>
        {% endif %}
        <a href="{{ path('app_home') }}">
{#            <img class="logo" src="{{ asset('images/MistralLogov.png') }}" alt="Logo">#}
            <img class="logo" src="{{ asset('images/Logo_white.png') }}" alt="Logo">
        </a>
        <div class="menu-icon">
            {% if app.user %}
                <div class="icon_header">
                    <a class="disconnect" href="{{ path('app_logout') }}">
                        <svg xmlns="http://www.w3.org/2000/svg" width="25" height="25" fill="currentColor" class="bi bi-power" viewBox="0 0 16 16">
                            <path d="M7.5 1v7h1V1z"/>
                            <path d="M3 8.812a5 5 0 0 1 2.578-4.375l-.485-.874A6 6 0 1 0 11 3.616l-.501.865A5 5 0 1 1 3 8.812"/>
                        </svg>
                    </a>
                </div>
            {% endif %}
        </div>
    </div>
</nav>


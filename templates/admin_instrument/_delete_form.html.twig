<form method="post" action="{{ path('app_admin_instrument_delete', {'id': instrument.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('Effacer' ~ instrument.id) }}">
    <button class="btn">Effacer</button>
</form>
